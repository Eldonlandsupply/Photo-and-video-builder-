name: Temporary Weekend at Mitch Final Transfer

on:
  pull_request:
    branches: [main]
    paths:
      - 'temporary/weekend-mitch-final-url.txt'

permissions:
  contents: read

jobs:
  transfer-final:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download rendered video
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p final
          curl -L --fail --retry 3 "$(cat temporary/weekend-mitch-final-url.txt)" -o final/Weekend_at_Mitchs_Ep1_LIVE_ACTION_FINAL.mp4
          test -s final/Weekend_at_Mitchs_Ep1_LIVE_ACTION_FINAL.mp4
          file final/Weekend_at_Mitchs_Ep1_LIVE_ACTION_FINAL.mp4
      - name: Upload final video
        uses: actions/upload-artifact@v4
        with:
          name: weekend-mitch-live-action-final
          path: final/Weekend_at_Mitchs_Ep1_LIVE_ACTION_FINAL.mp4
          if-no-files-found: error
          retention-days: 1
